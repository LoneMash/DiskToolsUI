<!-- MainWindow.xaml - Version 5.0 -->
<!-- Style C# Design Pro : header contextuel, sidebar gradient, glass cards -->

<Window x:Class="RunDeck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:RunDeck.Views"
        Title="{Binding WindowTitle}"
        Icon="/Icons/app.ico"
        Height="650" Width="1000"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Border Background="{StaticResource BackgroundDark}"
            CornerRadius="{StaticResource CornerRadiusWindow}"
            BorderBrush="{StaticResource BorderAccent}"
            BorderThickness="{StaticResource BorderThicknessWindow}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{StaticResource SidebarWidth}"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- SIDEBAR -->
            <views:SidebarView Grid.Column="0"/>

            <!-- ZONE DROITE -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="{StaticResource StatusBarHeight}"/>
                </Grid.RowDefinitions>

                <!-- HEADER CONTEXTUEL + boutons fenêtre -->
                <Border Grid.Row="0"
                        BorderBrush="{StaticResource BorderSubtle}"
                        BorderThickness="0,0,0,1"
                        Padding="20,14"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedAction.Name, FallbackValue='RunDeck'}"
                                       FontSize="{StaticResource FontSizeTitle}"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock Text="{Binding ActionDescription, FallbackValue='Sélectionnez une action dans la sidebar'}"
                                       FontSize="{StaticResource FontSizeSmall}"
                                       Foreground="{StaticResource TextMuted}"
                                       Margin="0,3,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <Button Content="─" Style="{StaticResource MinimizeButton}"
                                    Click="MinimizeButton_Click"/>
                            <Button Content="✕" Style="{StaticResource CloseButton}"
                                    Click="CloseButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- CONTENU : paramètres + résultats -->
                <Grid Grid.Row="1" Margin="{StaticResource MarginPage}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <views:ParametersView Grid.Row="0"/>
                    <views:ResultsView    Grid.Row="1"/>
                </Grid>

                <!-- BARRE DE STATUT -->
                <Border Grid.Row="2"
                        Background="{StaticResource BackgroundLight}"
                        BorderBrush="{StaticResource BorderSubtle}"
                        BorderThickness="{StaticResource BorderThicknessStatusBar}">
                    <TextBlock Text="{Binding StatusMessage}"
                               Foreground="{StaticResource TextMuted}"
                               Margin="{StaticResource PaddingStatusBar}"
                               VerticalAlignment="Center"
                               FontSize="{StaticResource FontSizeSmall}"/>
                </Border>
            </Grid>

            <!-- OVERLAY CHARGEMENT -->
            <Grid Grid.ColumnSpan="2"
                  Background="{StaticResource LoadingOverlay}"
                  Visibility="{Binding IsLoading,
                               Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⚡"
                               FontSize="{StaticResource LoadingIconSize}"
                               Foreground="{StaticResource AccentColor}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation" CenterX="24" CenterY="24"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    <TextBlock Text="Chargement en cours..."
                               FontSize="{StaticResource FontSizeLarge}"
                               Foreground="{StaticResource TextPrimary}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
