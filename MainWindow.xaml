<!-- MainWindow.xaml - Version 4.1 -->
<!-- Changelog : Suppression de Window.Resources, ressources désormais dans App.xaml -->

<Window x:Class="DiskToolsUi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:DiskToolsUi.Views"
        Title="{Binding WindowTitle}"
        Icon="/Icons/app.ico"
        Height="600" Width="900"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <!-- Plus de Window.Resources ici : tout est dans App.xaml -->

    <Border Background="{StaticResource BackgroundDark}"
            CornerRadius="{StaticResource CornerRadiusWindow}"
            BorderBrush="{StaticResource BorderAccent}"
            BorderThickness="{StaticResource BorderThicknessWindow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="{StaticResource TitleBarHeight}"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="{StaticResource StatusBarHeight}"/>
            </Grid.RowDefinitions>

            <!-- BARRE DE TITRE -->
            <Border Grid.Row="0"
                    Background="{StaticResource AccentColor}"
                    CornerRadius="{StaticResource CornerRadiusTitleBar}"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                        <TextBlock Text="⚡"
                                   FontSize="{StaticResource FontSizeTitle}"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding WindowTitle}"
                                   FontSize="{StaticResource FontSizeMedium}"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                        <Button Content="─" Style="{StaticResource MinimizeButton}"
                                Click="MinimizeButton_Click"/>
                        <Button Content="✕" Style="{StaticResource CloseButton}"
                                Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- CONTENU PRINCIPAL -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{StaticResource SidebarWidth}"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar gauche -->
                <views:SidebarView Grid.Column="0"/>

                <!-- Zone droite : paramètres + résultats -->
                <Grid Grid.Column="1" Margin="{StaticResource MarginPage}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <views:ParametersView Grid.Row="0"/>
                    <views:ResultsView    Grid.Row="1"/>
                </Grid>
            </Grid>

            <!-- BARRE DE STATUT -->
            <Border Grid.Row="2"
                    Background="{StaticResource BackgroundMedium}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="{StaticResource BorderThicknessStatusBar}">
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{StaticResource TextSecondary}"
                           Margin="{StaticResource PaddingStatusBar}"
                           VerticalAlignment="Center"
                           FontSize="{StaticResource FontSizeSmall}"/>
            </Border>

            <!-- OVERLAY CHARGEMENT -->
            <Grid Grid.RowSpan="3"
                  Background="{StaticResource LoadingOverlay}"
                  Visibility="{Binding IsLoading,
                               Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⚡"
                               FontSize="{StaticResource LoadingIconSize}"
                               Foreground="{StaticResource AccentColor}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation" CenterX="24" CenterY="24"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    <TextBlock Text="Chargement en cours..."
                               FontSize="{StaticResource FontSizeLarge}"
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
